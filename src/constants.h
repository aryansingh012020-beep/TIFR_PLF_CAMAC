/* Header file for lamps (see also lamps.h) -- The constants below can be changed as required*
 * consistency with lamps.h is essential                                                     */
#define MAX_PAR            1000
#define MAX_PSEUDO         800
#define MAX_TOTAL_PAR      (MAX_PAR+MAX_PSEUDO)
#define MAX_FNAME_LENGTH   255
